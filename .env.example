# 环境变量配置示例
# 复制为 .env 后填入实际值

# Supabase PostgreSQL 连接字符串（线上用）
# 获取方式：Supabase Dashboard → Project Settings → Database → Connection string → URI
# DATABASE_URL=postgresql://postgres.xxxxx:你的密码@db.xxxxx.supabase.co:5432/postgres

# 如果不设置 DATABASE_URL，本地开发会自动使用 SQLite（无需任何数据库安装）
